---
phase: 02-gpu-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Khor_Arika_Project_4/Problem_1/reduction_kernel.cu
  - Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu
  - Khor_Arika_Project_4/Problem_1/Makefile
autonomous: true
requirements:
  - P2-01
  - P2-02
  - P2-03
  - P2-04

must_haves:
  truths:
    - "A CUDA reduction kernel exists and is callable from host code."
    - "The gpu_mining_starter.cu program uses the GPU reduction kernel to find the minimum hash and nonce."
    - "The program's output for min_hash and min_nonce matches the expected output from the serial implementation."
    - "The program passes the autograder tests for Problem 2."
  artifacts:
    - path: "Khor_Arika_Project_4/Problem_1/reduction_kernel.cu"
      provides: "CUDA kernel for parallel reduction to find minimum hash and nonce"
      min_lines: 50
    - path: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
      provides: "Integration of reduction kernel, replaces CPU loop for finding minimum"
      contains: "reduction_kernel <<<"
    - path: "Khor_Arika_Project_4/Problem_1/Makefile"
      provides: "Compilation rules for reduction_kernel.cu"
      contains: "reduction_kernel.cu"
  key_links:
    - from: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
      to: "Khor_Arika_Project_4/Problem_1/reduction_kernel.cu"
      via: "kernel launch"
      pattern: "reduction_kernel <<< dimGrid, dimBlock >>>"
    - from: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
      to: "cudaMemcpy"
      via: "copying reduction result from device to host"
      pattern: "cudaMemcpy\(.*min_hash.*cudaMemcpyDeviceToHost\)"
---

<objective>
Implement an efficient parallel reduction on the GPU to find the nonce corresponding to the minimum hash.

Purpose: Accelerate the process of finding the minimum hash and its associated nonce, which is a critical step in the cryptocurrency mining problem. This phase moves a CPU-bound operation to the GPU for significant performance gains.
Output: Modified `gpu_mining_starter.cu` using a new `reduction_kernel.cu` that correctly identifies the minimum hash and nonce.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CUDA Parallel Reduction Kernel</name>
  <files>Khor_Arika_Project_4/Problem_1/reduction_kernel.cu</files>
  <behavior>
    - Test 1: Given an array of unsigned integers on the device, the kernel should return the minimum value and its index (which corresponds to the nonce).
    - Test 2: Handle edge cases like single-element arrays or arrays where the minimum is at the beginning/end.
    - Test 3: Ensure the kernel is efficient, potentially using shared memory and coalesced memory access.
  </behavior>
  <action>
    Create `reduction_kernel.cu` containing a CUDA kernel that performs a parallel reduction to find the minimum `unsigned int` value and its corresponding index (nonce).
    Consider a multi-pass or hierarchical reduction strategy for optimal performance with larger input sizes.
    The kernel should take the hash array and nonce array as input, and output the minimum hash and its corresponding nonce.
    Ensure that the kernel correctly handles different block and grid configurations, and that `cudaDeviceSynchronize()` is used after kernel launch to ensure results are ready before memory copy.
    Include necessary headers like `cuda_runtime.h` and `stdio.h`.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create {test_file} first</automated>
  </verify>
  <done>
    `reduction_kernel.cu` is created, compiles successfully (after Makefile update), and contains a working `__global__` function for parallel reduction.
    The kernel can identify the minimum value and its index from a sample array when invoked from a simple test harness (which will be part of the autograder later).
    (P2-01)
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Reduction Kernel into gpu_mining_starter.cu and Update Makefile</name>
  <files>
    Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu
    Khor_Arika_Project_4/Problem_1/Makefile
  </files>
  <action>
    Modify `Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu`:
    - Include `reduction_kernel.cu`.
    - Replace the existing CPU-based loop for finding `min_hash` and `min_nonce` (marked with `TODO Problem 2`) with a call to the new GPU reduction kernel.
    - Allocate necessary device memory for the reduction kernel's output (min_hash, min_nonce).
    - Copy the `min_hash` and `min_nonce` results from device to host memory after the kernel completes.
    - Add error checking using `err_check` for new CUDA operations.
    Modify `Khor_Arika_Project_4/Problem_1/Makefile`:
    - Add `reduction_kernel.cu` to the compilation targets and link it with `gpu_mining_starter.cu`.
  </action>
  <verify>
    <automated>cd Khor_Arika_Project_4/Problem_1 && make && ./gpu_mining_starter.out test_data/Problem_1_and_2/in_20k.csv 20000 100000 out.csv time.csv</automated>
  </verify>
  <done>
    `gpu_mining_starter.cu` compiles successfully with the integrated `reduction_kernel.cu`.
    The `TODO Problem 2` section in `gpu_mining_starter.cu` is removed and replaced by the kernel invocation and result handling.
    The `Makefile` is updated to properly compile and link `reduction_kernel.cu`.
    (P2-02)
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Correctness and Autograder Pass</name>
  <what-built>The integrated GPU reduction for finding the minimum hash and nonce.</what-built>
  <how-to-verify>
    1. Run the `gpu_mining_starter.out` executable with the provided test data:
       `cd Khor_Arika_Project_4/Problem_1 && ./gpu_mining_starter.out test_data/Problem_1_and_2/in_20k.csv 20000 100000 out.csv time.csv`
    2. Compare the `min_hash` and `min_nonce` output in `out.csv` with the expected output from a serial version (if available, or manually verify a small input).
    3. Run the autograder for Problem 2: `python3 Khor_Arika_Project_4/autograder_problem_4_2.py`
    4. Ensure the program's output (`min_hash` and `min_nonce`) matches the expected serial output for small inputs.
    5. Check that the autograder passes all tests for Problem 2.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues.</resume-signal>
  <done>
    The `gpu_mining_starter.out` program correctly identifies the minimum hash and nonce.
    The program's output matches the expected serial output.
    The `autograder_problem_4_2.py` script passes successfully.
    (P2-03, P2-04)
  </done>
</task>

</tasks>

<verification>
Overall phase verification involves running the updated `gpu_mining_starter.out` and ensuring it correctly performs the reduction task. The ultimate verification is passing the autograder for Problem 2.
</verification>

<success_criteria>
- The `gpu_mining_starter.cu` program is modified to use a GPU parallel reduction kernel.
- A new `reduction_kernel.cu` file is created and integrated into the build process.
- The program correctly identifies the minimum hash and its corresponding nonce.
- The solution for Problem 2 passes all tests in `autograder_problem_4_2.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-gpu-reduction/02-01-SUMMARY.md`
</output>