---
phase: 03-unit-testing-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - Khor_Arika_Project_4/tests/Makefile
  - Khor_Arika_Project_4/tests/test_convolution.cu
autonomous: true
requirements: [TEST-03]

must_haves:
  truths:
    - "A set of unit tests exists that verifies the convolution kernel's correctness."
    - "The convolution tests cover at least 3 cases: small matrix (1 block), large matrix, and 1x1 matrix."
    - "Executing `make test` in the `tests/` directory runs all tests (reduction and convolution) and reports results."
  artifacts:
    - path: "Khor_Arika_Project_4/tests/test_convolution.cu"
      provides: "Google Test suite for the GPU convolution kernel"
      contains: ["TEST(", "convolution_kernel <<<", "EXPECT_EQ"]
  key_links:
    - from: "Khor_Arika_Project_4/tests/test_convolution.cu"
      to: "Khor_Arika_Project_4/Problem_3/kernel.cu"
      via: "inclusion or linking"
      pattern: "convolution_kernel"
---

<objective>
Implement unit tests for the GPU convolution kernel and provide a unified command to run all project tests.

Purpose: To ensure the convolution algorithm correctly handles different matrix sizes and boundary conditions using shared memory tiling.
Output: A complete test suite including convolution tests and a `make test` target that executes both reduction and convolution tests.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Khor_Arika_Project_4/Problem_3/kernel.cu
@Khor_Arika_Project_4/tests/Makefile
@Khor_Arika_Project_4/common/support.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Convolution Unit Tests</name>
  <files>Khor_Arika_Project_4/tests/test_convolution.cu</files>
  <behavior>
    - Test 1: Small input matrix (16x16, one block). Verify against host serial convolution.
    - Test 2: Large input matrix (512x512). Verify correctness across multiple blocks.
    - Test 3: Edge case: 1x1 matrix (with padding to 5x5).
    - Test 4: Edge case: Matrix dimensions not multiple of BLOCK_SIZE (e.g., 33x33).
  </behavior>
  <action>
    - Create `test_convolution.cu`.
    - Include `gtest/gtest.h`, `cuda_runtime.h`, and `../common/support.h`.
    - Include `../Problem_3/kernel.cu` (which defines BLOCK_SIZE=16 and RADIUS=2).
    - Implement a host-side serial convolution function (with pre-padding) for verification.
    - Implement GTest test cases using the convolution kernel.
    - The host code should perform pre-padding (radius 2) before copying to the GPU (as the kernel expects padded input).
    - Verify that the GPU output matches the expected host results.
  </action>
  <verify>
    <automated>cd Khor_Arika_Project_4/tests && make test_convolution && ./test_convolution</automated>
  </verify>
  <done>
    `test_convolution.cu` is implemented with at least 3 test cases and successfully passes when executed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finalize Build System and Unified Testing</name>
  <files>Khor_Arika_Project_4/tests/Makefile</files>
  <action>
    - Update `Khor_Arika_Project_4/tests/Makefile` to include `test_convolution` target.
    - Update the `test` target to build and run both `test_reduction` and `test_convolution`.
    - Ensure all output is clear and reports overall success.
  </action>
  <verify>
    <automated>cd Khor_Arika_Project_4/tests && make test</automated>
  </verify>
  <done>
    `make test` builds and runs all unit tests, providing a summary of passes/failures.
  </done>
</task>

</tasks>

<verification>
Verify by running `make test` in the `tests/` directory. All tests for both kernels should pass.
</verification>

<success_criteria>
1. `make test` builds and executes both test binaries.
2. All convolution test cases pass.
3. Tests cover small, large, and edge-case matrix dimensions.
4. Correct use of GTest assertions.
5. Overall reporting shows 0 failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-unit-testing-integration/03-02-SUMMARY.md`
</output>
