---
phase: 03-convolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu
  - Khor_Arika_Project_4/Problem_3/kernel.cu
  - Khor_Arika_Project_4/Problem_3/support.cu
  - Khor_Arika_Project_4/Problem_3/support.h
  - Khor_Arika_Project_4/Problem_3/Makefile
autonomous: true
requirements:
  - P3-01
  - P3-02
  - P3-03

must_haves:
  truths:
    - "A CUDA convolution kernel using shared memory and tiling exists."
    - "The host code performs pre-padding (radius 2) before transferring data to the GPU."
    - "The GPU implementation produces identical output to the serial convolution."
    - "The Problem 3 autograder passes successfully."
  artifacts:
    - path: "Khor_Arika_Project_4/Problem_3/kernel.cu"
      provides: "Tiled 2D convolution kernel with shared memory"
      contains: "__shared__"
    - path: "Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu"
      provides: "Host application managing padding, memory transfers, and kernel launch"
      contains: "padMatrix"
    - path: "Khor_Arika_Project_4/Problem_3/Makefile"
      provides: "Build rules for the convolution project"
      contains: "convolution_CUDA"
  key_links:
    - from: "Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu"
      to: "Khor_Arika_Project_4/Problem_3/kernel.cu"
      via: "kernel launch"
      pattern: "convolution_kernel <<<"
    - from: "Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu"
      to: "padMatrix"
      via: "host-side padding before GPU transfer"
      pattern: "padMatrix"
---

<objective>
Implement a GPU-accelerated 2D convolution using CUDA with shared memory tiling and host-side pre-padding.

Purpose: Accelerate image processing operations by utilizing GPU parallelism. The use of shared memory tiling and pre-padding optimizes memory access patterns and simplifies kernel logic.
Output: A working `convolution_CUDA` executable that passes verification against the serial implementation and the autograder.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-convolution/03-convolution-CONTEXT.md
@Khor_Arika_Project_4/Problem_3/serial/convolution_serial.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Host-side Padding and Data Management</name>
  <files>
    Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu
    Khor_Arika_Project_4/Problem_3/support.cu
    Khor_Arika_Project_4/Problem_3/support.h
  </files>
  <action>
    - Copy `support.cu` and `support.h` from `Problem_1` and adapt them as needed (error checking, time measurement, CSV reading).
    - Create `convolution_CUDA.cu` to handle host-side logic:
      - Accept command line arguments: `n_row`, `n_col`, `input.csv`, `results.csv`, `time.csv` (per autograder requirements).
      - Read the input matrix from the CSV file into a 1D array.
      - Implement a `padMatrix` function that creates a new 1D array of size `(n_row + 4) * (n_col + 4)` with 2 pixels of zero-padding on all sides.
      - Initialize the 5x5 'X' filter matrix (per `convolution_serial.c`).
      - Allocate device memory for:
        - Padded input matrix (source).
        - Filter matrix.
        - Output matrix (unpadded, `n_row * n_col`).
      - Transfer padded input and filter to the device.
      - Set up 2D grid and 2D block dimensions (e.g., 16x16 or 32x32 blocks).
      - After kernel launch (Task 2), copy the output back to the host and save to `results.csv`.
      - Record and save execution time to `time.csv`.
  </action>
  <verify>
    <automated>nvcc -c Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu -o /tmp/conv.o</automated>
  </verify>
  <done>
    Host code correctly parses arguments, reads input, applies padding, and manages device memory.
    (P3-01)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Tiled Convolution Kernel with Shared Memory</name>
  <files>Khor_Arika_Project_4/Problem_3/kernel.cu</files>
  <behavior>
    - Test 1: Given a 1x1 input (padded to 5x5) and a filter, the output should match the single convolution result.
    - Test 2: For a 2048x2048 input, the output should be identical to the serial output (no boundary artifacts).
    - Test 3: Shared memory must be used for both the filter and the input tiles.
  </behavior>
  <action>
    Create `kernel.cu` with the convolution kernel:
    - Define `BLOCK_SIZE` (e.g., 16).
    - Shared memory allocation:
      - `__shared__ int filter_s[25]`
      - `__shared__ int tile_s[BLOCK_SIZE + 4][BLOCK_SIZE + 4]`
    - Each block collaboratively loads the 5x5 filter into `filter_s`.
    - Each block collaboratively loads its corresponding input tile + halo (radius 2) into `tile_s`.
    - Synchronize threads.
    - Each thread computes one output pixel by iterating over the 5x5 shared filter and the tile in shared memory.
    - Write the result to the global output matrix.
  </action>
  <verify>
    <automated>nvcc -c Khor_Arika_Project_4/Problem_3/kernel.cu -o /tmp/kernel.o</automated>
  </verify>
  <done>
    Convolution kernel correctly implements tiling and shared memory logic.
    Output pixel calculation correctly maps to the padded input indices.
    (P3-02)
  </done>
</task>

<task type="auto">
  <name>Task 3: Makefile Update and Autograder Verification</name>
  <files>
    Khor_Arika_Project_4/Problem_3/Makefile
    Khor_Arika_Project_4/Problem_3/P4-3.sbatch
  </files>
  <action>
    - Finalize `Khor_Arika_Project_4/Problem_3/Makefile` to ensure it correctly compiles `convolution_CUDA.cu`, `support.cu`, and `kernel.cu` into the `convolution_CUDA` executable.
    - Ensure `convolution_CUDA.cu` includes `kernel.cu` or they are linked properly.
    - Update `P4-3.sbatch` to:
      1. Compile the code.
      2. Run the executable with the provided test data (`test_data/Problem_3/mat_input.csv`).
      3. Compare the output `results.csv` with `test_data/Problem_3/mat_p3_output.csv` using `diff`.
    - Run the problem-specific autograder: `python3 Khor_Arika_Project_4/autograder_problem_4_3.py`.
  </action>
  <verify>
    <automated>python3 Khor_Arika_Project_4/autograder_problem_4_3.py</automated>
  </verify>
  <done>
    `convolution_CUDA` produces identical output to the serial version.
    The `autograder_problem_4_3.py` script passes successfully (1/1 problems correct).
    (P3-03)
  </done>
</task>

</tasks>

<verification>
Verify by running the Problem 3 autograder. Comparison with serial output via `diff` provides additional confidence.
</verification>

<success_criteria>
- Host code correctly implements pre-padding.
- Kernel correctly uses shared memory tiling for filter and input data.
- GPU output matches serial output exactly.
- `autograder_problem_4_3.py` passes all tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-convolution/03-01-SUMMARY.md`
</output>
