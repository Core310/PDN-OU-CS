---
phase: 02-gpu-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "Khor_Arika_Project_4/Problem_1/reduction_kernel.cu",
  "Khor_Arika_Project_4/Problem_1/reduction_kernel.h",
  "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
]
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "Problem 1 (mining) finds the correct minimum hash and nonce on the GPU."
    - "No host-side loop is used for the final aggregation of block-level results."
    - "Unnecessary large memory copies from device to host are removed."
    - "Autograder for Problem 1 passes."
  artifacts:
    - path: "Khor_Arika_Project_4/Problem_1/reduction_kernel.cu"
      provides: "Atomic-based parallel reduction"
      contains: "atomicCAS"
    - path: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
      provides: "On-device reduction flow"
      contains: "cudaMemcpyDeviceToHost"
  key_links:
    - from: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
      to: "Khor_Arika_Project_4/Problem_1/reduction_kernel.cu"
      via: "Kernel launch with global minimum result pointer"
---

<objective>
Implement a fully on-device parallel reduction for the mining application (Problem 1) using atomic operations to find the minimum hash and its corresponding nonce. This optimizes performance by eliminating host-side aggregation and reducing device-to-host data transfers.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gpu-reduction/02-gpu-reduction-CONTEXT.md
@Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu
@Khor_Arika_Project_4/Problem_1/reduction_kernel.cu
@Khor_Arika_Project_4/Problem_1/reduction_kernel.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update reduction kernel to use atomicCAS</name>
  <files>Khor_Arika_Project_4/Problem_1/reduction_kernel.cu, Khor_Arika_Project_4/Problem_1/reduction_kernel.h</files>
  <action>
    Modify `reduction_kernel.cu` to use `atomicCAS` for global minimum aggregation.
    1.  Pack `min_hash` (32 bits) and `min_nonce` (32 bits) into a single `unsigned long long` value (hash in higher bits, nonce in lower bits).
    2.  Update the `reduction_kernel` to take a pointer to a single `unsigned long long` global result.
    3.  In `tid == 0` of each block, use a `while(true)` loop with `atomicCAS` to update the global result if the candidate's `min_hash` is smaller than the currently stored `min_hash`.
    4.  Update `reduction_kernel.h` with the new function signature.
    5.  Reference decision ID D-02 from Phase 2 context for atomic packing strategy.
  </action>
  <verify>
    `nvcc -c Khor_Arika_Project_4/Problem_1/reduction_kernel.cu -I Khor_Arika_Project_4/common` compiles without errors.
  </verify>
  <done>Reduction kernel uses atomicCAS for global aggregation and correctly packs/unpacks the hash-nonce pair.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor host code for full GPU reduction</name>
  <files>Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu</files>
  <action>
    Update `gpu_mining_starter.cu` to implement the new on-device reduction flow.
    1.  Remove the allocation and copying of full `hash_array` and `nonce_array` back to host before reduction (per Gray Area 3 decision in CONTEXT.md).
    2.  Remove allocation of per-block results (`d_min_hash_out`, `d_min_nonce_out`).
    3.  Allocate a single `unsigned long long` on the device for the global result.
    4.  Initialize the global result with packed `MAX` and `UINT_MAX`.
    5.  Update the kernel launch for `reduction_kernel` to pass the global result pointer.
    6.  Copy only the final 64-bit result back to the host after synchronization.
    7.  Unpack and store the final `min_hash` and `min_nonce`.
    8.  Remove the host-side reduction loop.
    9. Ensure all device memory is freed correctly.
  </action>
  <verify>
    `make -C Khor_Arika_Project_4/Problem_1` compiles and produces `gpu_mining_starter`.
  </verify>
  <done>Host code manages a single global result on the device and skips unnecessary data transfers.</done>
</task>

<task type="auto">
  <name>Task 3: Verification and Benchmarking</name>
  <files>Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu</files>
  <action>
    Verify the correctness and performance of the optimized implementation.
    1.  Run the Problem 1 autograder: `python3 Khor_Arika_Project_4/autograder_problem_4_1.py`.
    2.  Verify that it passes for both 1k and 20k trial test cases.
    3.  Compare the execution time with the original version (recorded in ROADMAP or previous SUMMARIES) to confirm speedup for large input sizes.
  </action>
  <verify>
    `python3 Khor_Arika_Project_4/autograder_problem_4_1.py` output shows "Final result: Passed".
  </verify>
  <done>Optimized implementation passes the autograder and shows improved performance.</done>
</task>

</tasks>

<success_criteria>
1. `gpu_mining_starter` compiles correctly and passes all autograder tests for Problem 1.
2. The source code contains no host-side aggregation loops for the reduction.
3. The application avoids copying the full hash/nonce arrays back to the host.
4. Correct min_hash and min_nonce are reported for various trial sizes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-gpu-reduction/02-01-SUMMARY.md`
</output>
