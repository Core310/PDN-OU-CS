---
phase: 03-unit-testing-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Khor_Arika_Project_4/tests/Makefile
  - Khor_Arika_Project_4/tests/test_reduction.cu
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "A unit test framework (Google Test) is integrated into the project's build system."
    - "A set of unit tests exists that verifies the parallel reduction kernel's correctness."
    - "The reduction tests cover at least 3 cases: small input (1 block), large input, and non-power-of-two input size."
    - "Executing `make test_reduction` in the `tests/` directory runs the tests and reports results."
  artifacts:
    - path: "Khor_Arika_Project_4/tests/Makefile"
      provides: "Build system for unit tests linking Google Test"
      contains: ["-lgtest", "-lpthread"]
    - path: "Khor_Arika_Project_4/tests/test_reduction.cu"
      provides: "Google Test suite for the GPU reduction kernel"
      contains: ["TEST(", "reduction_kernel <<<", "EXPECT_EQ"]
  key_links:
    - from: "Khor_Arika_Project_4/tests/test_reduction.cu"
      to: "Khor_Arika_Project_4/Problem_1/reduction_kernel.cu"
      via: "inclusion or linking"
      pattern: "reduction_kernel"
---

<objective>
Initialize the Google Test (GTest) framework and implement unit tests for the optimized GPU reduction kernel.

Purpose: To ensure the correctness of the reduction algorithm across various input sizes and edge cases.
Output: A functional testing environment in `Khor_Arika_Project_4/tests/` and a passing suite of reduction tests.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Khor_Arika_Project_4/Problem_1/reduction_kernel.cu
@Khor_Arika_Project_4/common/support.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup GTest Testing Environment</name>
  <files>Khor_Arika_Project_4/tests/Makefile</files>
  <action>
    - Create the `Khor_Arika_Project_4/tests/` directory.
    - Create a `Makefile` in that directory that:
      - Uses `nvcc` for compilation.
      - Includes `/usr/include/gtest`.
      - Links against `/usr/lib/x86_64-linux-gnu/libgtest.a` and `-lpthread`.
      - Includes `../common` for `support.h`.
      - Defines a target `test_reduction` and an alias `test`.
  </action>
  <verify>
    <automated>ls Khor_Arika_Project_4/tests/Makefile</automated>
  </verify>
  <done>
    A functional Makefile exists that can link with GTest and include CUDA/common headers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Reduction Unit Tests</name>
  <files>Khor_Arika_Project_4/tests/test_reduction.cu</files>
  <behavior>
    - Test 1: Small input (size=1024, one block). All elements sum to known value.
    - Test 2: Large input (size=1,000,000). Verify GPU result matches host-side serial reduction.
    - Test 3: Edge case: Input size not a power of two (e.g., size=10,000).
    - Test 4: Edge case: Single element input (size=1).
  </behavior>
  <action>
    - Create `test_reduction.cu`.
    - Include `gtest/gtest.h`, `cuda_runtime.h`, and `../common/support.h`.
    - Include or link `../Problem_1/reduction_kernel.cu`.
    - Implement a host-side serial reduction function for verification.
    - Implement the `main` function for GTest (`InitGoogleTest` and `RUN_ALL_TESTS`).
    - Use `cudaMalloc`, `cudaMemcpy`, and the `reduction_kernel` launch (with 1024 threads/block).
    - Verify that the minimum value (hash and nonce) found by the kernel matches the host serial result.
  </action>
  <verify>
    <automated>cd Khor_Arika_Project_4/tests && make test_reduction && ./test_reduction</automated>
  </verify>
  <done>
    `test_reduction.cu` is implemented with at least 3 test cases and successfully passes when executed.
  </done>
</task>

</tasks>

<verification>
Verify by navigating to `Khor_Arika_Project_4/tests/` and running `make test_reduction`. The output should show Google Test progress and "PASSED" for all tests.
</verification>

<success_criteria>
1. `make test_reduction` builds without errors.
2. `./test_reduction` runs and all test cases pass.
3. Tests cover small, large, and non-power-of-two inputs.
4. Correct use of GTest assertions (`EXPECT_EQ`, etc.).
</success_criteria>

<output>
After completion, create `.planning/phases/03-unit-testing-integration/03-01-SUMMARY.md`
</output>
