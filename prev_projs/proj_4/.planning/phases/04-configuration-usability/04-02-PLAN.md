---
phase: 04-configuration-usability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [Khor_Arika_Project_4/Problem_3/kernel.cu, Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu, Khor_Arika_Project_4/Problem_4/convolution_maxpooling.cu, Khor_Arika_Project_4/Problem_4/kernel.cu]
autonomous: true
requirements: [CONF-02]
---

<objective>
Enable configurable BLOCK_SIZE and RADIUS for Problem 3 & 4 (Convolution and Maxpooling).

Purpose: Allow flexible tile sizes and filter radii for performance tuning and different problem sizes.
Output: Programs that accept optional arguments for block size and filter radius, and kernels that use dynamic shared memory.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Khor_Arika_Project_4/Problem_3/kernel.cu
@Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu
@Khor_Arika_Project_4/Problem_4/convolution_maxpooling.cu
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update convolution kernels with dynamic shared memory</name>
  <files>Khor_Arika_Project_4/Problem_3/kernel.cu, Khor_Arika_Project_4/Problem_4/convolution_maxpooling.cu, Khor_Arika_Project_4/Problem_4/kernel.cu</files>
  <action>
    1. Update convolution_kernel signature to accept `block_size` and `radius` as arguments.
    2. Replace statically-sized `__shared__ filter_s[25]` and `__shared__ tile_s[...]` with `extern __shared__ int s_data[]`.
    3. Partition `s_data` into `filter_s` and `tile_s` using the dynamic `radius` and `block_size` values.
    4. Update the filter loading logic to handle a filter size of `(2 * radius + 1) * (2 * radius + 1)`.
    5. Update the tile loading logic and convolution loops to use `block_size` and `radius` variables instead of macros.
    6. Update maxpooling_kernel in convolution_maxpooling.cu to accept `radius` as an argument and use it in the window loops.
  </action>
  <verify>
    Kernel files compile correctly.
  </verify>
  <done>
    Kernels no longer rely on BLOCK_SIZE or RADIUS macros.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update host-side code with configurable parameters</name>
  <files>Khor_Arika_Project_4/Problem_3/convolution_CUDA.cu, Khor_Arika_Project_4/Problem_4/convolution_maxpooling.cu</files>
  <action>
    1. Update the usage message to include [BLOCK_SIZE] and [RADIUS] (as 6th and 7th arguments, i.e., argv[6] and argv[7]).
    2. Parse optional 6th (argv[6]) and 7th (argv[7]) arguments. Defaults: BLOCK_SIZE = 16, RADIUS = 2.
    3. Update `padMatrix` function to accept `radius` as an argument.
    4. Dynamically allocate `filterMatrix_h` with size `(2 * radius + 1) * (2 * radius + 1) * sizeof(int)`.
    5. Initialize the filter: if `radius == 2`, use the existing 5x5 'X' pattern; otherwise, use an identity filter (center element = 1).
    6. Update kernel launches (convolution_kernel and maxpooling_kernel) to pass the new arguments and the correct dynamic shared memory size.
    7. Shared memory size for convolution: `sizeof(int) * ((block_size + 2 * radius) * (block_size + 2 * radius) + (2 * radius + 1) * (2 * radius + 1))`.
  </action>
  <verify>
    <automated>
      cd Khor_Arika_Project_4/Problem_3 && make && ./convolution_maxpooling_CUDA 16 16 ../../test_data/Problem_3/mat_input.csv out.csv time.csv 16 3
    </automated>
  </verify>
  <done>
    Programs run correctly with different BLOCK_SIZE and RADIUS values.
  </done>
</task>

</tasks>

<verification>
Verify Problem 3 and 4 with default values (should pass autograder) and non-default values (should not crash and produce reasonable output).
</verification>

<success_criteria>
1. User can specify BLOCK_SIZE and RADIUS from the command line.
2. Kernels use dynamic shared memory.
3. Host side correctly pads and initializes filter based on RADIUS.
</success_criteria>

<output>
After completion, create `.planning/phases/04-configuration-usability/04-02-SUMMARY.md`
</output>
