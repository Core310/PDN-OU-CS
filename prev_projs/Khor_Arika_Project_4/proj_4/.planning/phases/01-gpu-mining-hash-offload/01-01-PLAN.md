---
phase: 01-gpu-mining-hash-offload
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - "Khor_Arika_Project_4/Problem_1/hash_kernel.cu"
  - "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
  - "Khor_Arika_Project_4/Problem_1/Makefile"
autonomous: false
requirements:
  - P1-01
  - P1-02
  - P1-03
  - P1-04
must_haves:
  truths:
    - "The GPU implementation computes the same hash values as the serial CPU implementation for the same nonce and transaction data."
    - "The main host application successfully launches the hash kernel on the GPU."
    - "The `TODO` for hash generation in `gpu_mining_starter.cu` is removed."
  artifacts:
    - path: "Khor_Arika_Project_4/Problem_1/hash_kernel.cu"
      provides: "Parallel hash generation CUDA kernel."
      contains: "__global__ void generate_hash_kernel"
    - path: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
      provides: "Host-side orchestration for the hash kernel."
      contains: "generate_hash_kernel<<<"
    - path: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter"
      provides: "Compiled executable for the GPU mining application."
  key_links:
    - from: "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
      to: "Khor_Arika_Project_4/Problem_1/hash_kernel.cu"
      via: "kernel launch `<<<...>>>`"
      pattern: "generate_hash_kernel<<<"
    - from: "gpu_mining_starter.cu (host)"
      to: "GPU device memory"
      via: "cudaMemcpy"
      pattern: "cudaMemcpy.*cudaMemcpyHostToDevice"
---

<objective>
To accelerate the cryptocurrency mining process by offloading the computationally intensive hash generation from the CPU to the GPU. This involves implementing a new CUDA kernel and integrating it into the existing host application, replacing the current serial implementation.

**Purpose:** Fulfill the primary requirement of Problem 1 by leveraging GPU parallelism for performance gains.
**Output:** A modified GPU mining application that correctly generates hashes on the GPU.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu
@Khor_Arika_Project_4/Problem_1/hash_kernel.cu
@Khor_Arika_Project_4/Problem_1/serial/serial_mining.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Parallel Hash Kernel</name>
  <files>
    - "Khor_Arika_Project_4/Problem_1/hash_kernel.cu"
    - "Khor_Arika_Project_4/Problem_1/support.h"
  </files>
  <behavior>
    - The `generate_hash_kernel` should correctly calculate the hash for a given nonce and transaction data.
    - Each thread should work on a different nonce from the `nonce_array`.
    - The resulting hash should be stored in the corresponding index of the `hash_array`.
    - The logic should mirror the `generate_hash` function in the serial C code (`serial_mining.c`).
  </behavior>
  <action>
    Modify `Khor_Arika_Project_4/Problem_1/hash_kernel.cu` to implement the `generate_hash_kernel`.

    1.  **Define Kernel Signature:** The kernel should accept pointers to device memory for the transaction list (`device_transactions`), the nonce array (`device_nonce_array`), the output hash array (`device_hash_array`), and the number of transactions (`n_transactions`).
    2.  **Implement Thread Logic:** Inside the kernel, calculate the unique global thread ID using `blockIdx.x * blockDim.x + threadIdx.x`. This ID will correspond to the index of the nonce this thread should process.
    3.  **Replicate Serial Logic:** Port the hash generation logic from the `generate_hash` function in `Khor_Arika_Project_4/Problem_1/serial/serial_mining.c`. The core logic is a loop over transactions, performing XOR and multiplication.
    4.  **Store Result:** Write the final calculated hash value to the `device_hash_array` at the thread's unique global ID index.
    5.  **Add Function Prototype:** Add the prototype for `generate_hash_kernel` to `support.h` so it can be called from `gpu_mining_starter.cu`.
  </action>
  <verify>
    <automated>The file `Khor_Arika_Project_4/Problem_1/hash_kernel.cu` should contain a non-empty `generate_hash_kernel` function.</automated>
  </verify>
  <done>
    - The `TODO` in `hash_kernel.cu` is removed.
    - The `generate_hash_kernel` function is fully implemented.
    - The kernel's logic is a parallel equivalent of the serial version's `generate_hash` function.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Hash Kernel into Host Application</name>
  <files>
    - "Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu"
  </files>
  <action>
    Modify `Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu` to replace the CPU-based hash generation loop with a call to the new `generate_hash_kernel`.

    1.  **Allocate Device Memory:** Before the kernel launch, allocate memory on the GPU for the output hash array (`device_hash_array`) using `cudaMalloc`. The size should be `n_nonce * sizeof(unsigned int)`. Use the `err_check` convention for error handling.
    2.  **Configure Kernel Launch:** Define the grid and block dimensions (`dimGrid`, `dimBlock`). `dimBlock` can be a constant like 256. `dimGrid` should be calculated as `(n_nonce + dimBlock.x - 1) / dimBlock.x` to ensure enough threads are launched for all nonces.
    3.  **Launch Kernel:** Remove the `for` loop that currently calculates hashes on the CPU (it's marked with `// TODO Problem 1`). Replace it with a launch of `generate_hash_kernel<<<dimGrid, dimBlock>>>(...)`, passing the required device pointers and parameters.
    4.  **Copy Results Back (for now):** After the kernel launch and a `cudaDeviceSynchronize()`, copy the `device_hash_array` from the GPU back to a newly allocated host array (`host_hash_array`) using `cudaMemcpy`. This is necessary for the next step (finding the minimum) which is still on the CPU.
    5.  **Update Minimum Search:** Modify the subsequent `for` loop (marked with `// TODO Problem 2`) to iterate over the `host_hash_array` instead of calling `generate_hash` repeatedly.
    6.  **Free Memory:** Add a `cudaFree(device_hash_array)` call at the end of the program, before `cudaDeviceReset()`.
  </action>
  <verify>
    <automated>The file `Khor_Arika_Project_4/Problem_1/gpu_mining_starter.cu` should contain the string `generate_hash_kernel<<<` and no longer contain the old `for` loop for hash generation.</automated>
  </verify>
  <done>
    - The serial hash generation loop is removed from `gpu_mining_starter.cu`.
    - The application now launches the `generate_hash_kernel` on the GPU.
    - Necessary `cudaMalloc`, `cudaMemcpy`, and `cudaFree` calls for the hash array are present.
    - The subsequent "find minimum" loop is updated to use the results from the GPU.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Build and Verify Correctness</name>
  <files>
    - "Khor_Arika_Project_4/Problem_1/Makefile"
    - "Khor_Arika_Project_4/Problem_1/gpu_mining_starter"
  </files>
  <what-built>
    The agent has implemented the GPU hash offloading as per the plan. The code should now be ready to compile and run, with the hash calculation happening on the GPU.
  </what-built>
  <how-to-verify>
    1.  **Compile the code:**
        - Navigate to the `Khor_Arika_Project_4/Problem_1/` directory.
        - Run `make clean && make`. The code should compile without errors and produce an executable named `gpu_mining_starter`.
    2.  **Run the GPU version:**
        - Execute the program with test data: `./gpu_mining_starter ../../test_data/Problem_1_and_2/in_20k.csv 20000 1 5000000 1`
        - Note the output, specifically the "Minimum Hash" and "Nonce" values printed to the console.
    3.  **Compile and run the serial version for comparison:**
        - Navigate to the `Khor_Arika_Project_4/Problem_1/serial/` directory.
        - Run `make clean && make`.
        - Execute the serial program: `./serial_mining ../../../test_data/Problem_1_and_2/in_20k.csv 20000 1 5000000 1`
    4.  **Compare outputs:**
        - The "Minimum Hash" and "Nonce" values from the GPU version (Step 2) must exactly match the values from the serial CPU version (Step 3).
    5.  **Run the autograder (optional but recommended):**
        - Navigate to the `Khor_Arika_Project_4/` directory.
        - Run `python autograder_problem_4_1.py`. The test for Problem 1 should pass.
  </how-to-verify>
  <resume-signal>Type "approved" if the GPU and serial outputs match. If they do not, paste the differing output logs.</resume-signal>
</task>

</tasks>

<verification>
- **Correctness:** The final `min_hash` and `nonce` values produced by the GPU-accelerated code must be identical to the values produced by the original serial implementation for the same input.
- **Performance:** While not a strict requirement for this phase, a noticeable speedup in execution time for the GPU version compared to the serial version is expected.
</verification>

<success_criteria>
- The `P1-*` requirements are met.
- The `autograder_problem_4_1.py` script reports a passing grade for Problem 1.
- The CPU-based `for` loop for hash generation is successfully removed and replaced by a parallel GPU kernel launch.
- The application remains functionally correct.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gpu-mining-hash-offload/01-01-SUMMARY.md`
</output>
