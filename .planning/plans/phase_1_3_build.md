# Plan: Phase 1.3 - Build System and Verification

## Objective
Update the Makefile to support all variants and verify compilation.

## Tasks
1. [ ] Update `Project_3_Problems/Khor_Arika_Project_3/Problem_2/Makefile`.
2. [ ] Add targets for `starter`, `critical`, `atomic`, `locks`, and `schedule`.
3. [ ] Use `-O3 -fopenmp -Wall -std=c99 -lm` flags.
4. [ ] Run `make all` and verify no errors.

## Verification
- All 5 binaries exist and are executable.
