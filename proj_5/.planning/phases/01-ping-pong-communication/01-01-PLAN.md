---
phase: 01-ping-pong-communication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Problem_1/pingpong_MPI.c
  - Problem_1/Makefile
  - Problem_1/pingpong_samenode.sbatch
  - Problem_1/pingpong_diffnode.sbatch
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04

must_haves:
  truths:
    - "The `pingpong_MPI` executable correctly performs array bouncing between two MPI processes for 1000 iterations."
    - "The program measures and outputs the communication time for specified array sizes (e.g., 2^18, 2^19, 2^20 bytes)."
    - "pingpong_samenode.sbatch successfully executes the program on a single node and collects time data."
    - "pingpong_diffnode.sbatch successfully executes the program across different nodes and collects time data."
    - "The `Makefile` properly compiles `pingpong_MPI.c` into an executable."
  artifacts:
    - path: "Problem_1/pingpong_MPI.c"
      provides: "MPI Ping-Pong logic with array bouncing and time measurement"
      contains: "MPI_Send"
      contains: "MPI_Recv"
      contains: "MPI_Wtime"
    - path: "Problem_1/Makefile"
      provides: "Build instructions for pingpong_MPI"
      contains: "mpicc"
      contains: "pingpong_MPI"
    - path: "Problem_1/pingpong_samenode.sbatch"
      provides: "SLURM script for single-node execution"
      contains: "mpirun"
      contains: "srun"
    - path: "Problem_1/pingpong_diffnode.sbatch"
      provides: "SLURM script for multi-node execution"
      contains: "mpirun"
      contains: "srun"
  key_links:
    - from: "Problem_1/pingpong_MPI.c"
      to: "MPI Library functions (MPI_Send, MPI_Recv, MPI_Wtime)"
      via: "MPI calls"
      pattern: "MPI_"
    - from: "Problem_1/pingpong_MPI.c"
      to: "Output files"
      via: "file I/O for timing data"
      pattern: "fprintf|printf"
    - from: "Problem_1/Makefile"
      to: "Problem_1/pingpong_MPI.c"
      via: "compilation command"
      pattern: "mpicc -o pingpong_MPI pingpong_MPI.c"
    - from: "Problem_1/pingpong_samenode.sbatch"
      to: "Problem_1/pingpong_MPI"
      via: "execution command"
      pattern: "mpirun|srun.*pingpong_MPI"
    - from: "Problem_1/pingpong_diffnode.sbatch"
      to: "Problem_1/pingpong_MPI"
      via: "execution command"
      pattern: "mpirun|srun.*pingpong_MPI"
---

<objective>
Implement the Ping-Pong communication problem for MPI, measuring communication times for array bouncing on both single and multi-node configurations.

Purpose: To fulfill the requirements of Project 5, Problem 1, demonstrating proficiency in basic MPI communication patterns and performance measurement.
Output: Working `pingpong_MPI.c` source code, a `Makefile` for compilation, and `sbatch` scripts for execution on a SLURM cluster.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Problem_1/pingpong_MPI.c
@Problem_1/Makefile
@Problem_1/pingpong_samenode.sbatch
@Problem_1/pingpong_diffnode.sbatch
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Ping-Pong Array Bouncing Logic and Time Measurement</name>
  <files>Problem_1/pingpong_MPI.c</files>
  <behavior>
    - Test 1: Program executes successfully with 2 MPI processes.
    - Test 2: Array of specified size is correctly allocated and used for ping-pong.
    - Test 3: Ping-pong exchange occurs 1000 times (or specified iteration count).
    - Test 4: Communication time is measured using MPI_Wtime for each iteration and overall.
    - Test 5: Output includes ranks, message size, and measured times.
  </behavior>
  <action>
    Modify `Problem_1/pingpong_MPI.c`.
    - Initialize MPI environment.
    - Get process rank and total number of processes. Ensure exactly 2 processes are used.
    - Implement a loop for 1000 iterations (or a configurable count).
    - Inside the loop, implement ping-pong logic where rank 0 sends an array to rank 1, and rank 1 sends it back to rank 0.
    - Use `MPI_Send` and `MPI_Recv` with `MPI_BYTE` or specific data types.
    - For array sizes, use powers of 2 (e.g., 2^18, 2^19, 2^20 bytes).
    - Use `MPI_Wtime` to accurately measure the time taken for each ping-pong exchange.
    - Print out the results including message size, number of processes, and measured time.
  </action>
  <verify>
    <automated>mpicc Problem_1/pingpong_MPI.c -o Problem_1/pingpong_MPI; mpirun -np 2 Problem_1/pingpong_MPI</automated>
    Output should show timing results for 1000 iterations.
  </verify>
  <done>
    `pingpong_MPI.c` compiles without errors and executes, performing array bouncing 1000 times between two processes, and printing accurate communication times for the specified array sizes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Makefile and SLURM Sbatch Scripts</name>
  <files>
    Problem_1/Makefile
    Problem_1/pingpong_samenode.sbatch
    Problem_1/pingpong_diffnode.sbatch
  </files>
  <action>
    Modify `Problem_1/Makefile`:
    - Ensure it compiles `pingpong_MPI.c` using `mpicc` and creates an executable named `pingpong_MPI`.
    - Add a `clean` target.

    Modify `Problem_1/pingpong_samenode.sbatch`:
    - Set `#SBATCH` directives for a single node, e.g., `nodes=1`, `ntasks-per-node=2`.
    - Add `module load ...` commands for MPI if necessary for the environment (referencing `.planning/STRUCTURE.md` or existing `sbatch` files for common modules).
    - Execute the compiled `pingpong_MPI` program using `mpirun` or `srun`, passing parameters for message size as required (e.g., looping through 2^18, 2^19, 2^20 in bytes).
    - Redirect output to a suitable results file (e.g., `results/problem1_samenode_times.csv`).

    Modify `Problem_1/pingpong_diffnode.sbatch`:
    - Set `#SBATCH` directives for multiple nodes, e.g., `nodes=2`, `ntasks-per-node=1`.
    - Add `module load ...` commands for MPI.
    - Execute the compiled `pingpong_MPI` program using `mpirun` or `srun`, passing parameters for message size.
    - Redirect output to a suitable results file (e.g., `results/problem1_diffnode_times.csv`).
  </action>
  <verify>
    <automated>
      cd Problem_1 && make && ls pingpong_MPI &&
      cat pingpong_samenode.sbatch | grep "#SBATCH --nodes=1" &&
      cat pingpong_diffnode.sbatch | grep "#SBATCH --nodes=2"
    </automated>
    `Makefile` should compile successfully, and `sbatch` files should contain correct node configurations and execution commands.
  </verify>
  <done>
    `Makefile` is updated and correctly compiles `pingpong_MPI.c`. Both `pingpong_samenode.sbatch` and `pingpong_diffnode.sbatch` are configured to execute the MPI program on appropriate node configurations, passing the required message sizes, and redirecting output.
  </done>
</task>

</tasks>

<verification>
Execute both `pingpong_samenode.sbatch` and `pingpong_diffnode.sbatch` on the cluster. Check the generated output files for accurate timing measurements across different array sizes and node configurations.
</verification>

<success_criteria>
- The `Problem_1/pingpong_MPI.c` file contains the complete and correct MPI ping-pong implementation with array bouncing and timing.
- The `Problem_1/Makefile` successfully builds the `pingpong_MPI` executable.
- The `Problem_1/pingpong_samenode.sbatch` and `Problem_1/pingpong_diffnode.sbatch` files are correctly configured for single and multi-node execution, respectively, and generate timing data.
- All specified requirements from the roadmap for Milestone 1 are met.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ping-pong-communication/01-01-SUMMARY.md`
</output>
