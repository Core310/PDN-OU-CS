---
phase: 05-report-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reports/
  - reports/data/
  - reports/analysis_utils.py
autonomous: true
requirements:
  - MILESTONE5-R1
user_setup: []

must_haves:
  truths:
    - "A 'reports/' directory exists in the project root."
    - "All necessary raw timing CSVs (`time_*.csv`) are consolidated in 'reports/data/'."
    - "A Python utility script 'reports/analysis_utils.py' is created with helper functions for data processing."
  artifacts:
    - path: "reports/"
      provides: "Container for all report-related assets"
    - path: "reports/data/"
      provides: "Consolidated raw timing data"
    - path: "reports/analysis_utils.py"
      provides: "Python helper functions for analysis"
      min_lines: 20
  key_links:
    - from: "reports/analysis_utils.py"
      to: "reports/data/*.csv"
      via: "pandas.read_csv"
      pattern: "pd\.read_csv"
---

<objective>
Set up the analysis environment and consolidate all raw timing data needed for the final report.

Purpose: To ensure all required timing data from the MPI problems is centrally located and a foundational utility script for common analysis tasks is available, enabling subsequent data analysis and report generation.
Output: A structured 'reports/' directory with consolidated raw data and an initial Python utility script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reports directory and data subdirectory</name>
  <files>
    reports/
    reports/data/
  </files>
  <action>
    Create a new top-level directory named `reports/`.
    Inside `reports/`, create another subdirectory named `data/`.
  </action>
  <verify>
    <automated>ls -d reports/ && ls -d reports/data/</automated>
  </verify>
  <done>
    `reports/` and `reports/data/` directories exist in the project root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidate timing data files</name>
  <files>
    reports/data/problem1_times_diffnode.csv
    reports/data/problem1_times_samenode.csv
    reports/data/problem2_times.csv
    reports/data/problem3_times.csv
    reports/data/problem4_times.csv
  </files>
  <action>
    Locate the generated timing CSV files for each problem and copy them into `reports/data/`.
    Specifically, look for files like:
    - `Problem_1/pingpong_diffnode.sbatch` and `Problem_1/pingpong_samenode.sbatch` produce outputs. The `ROADMAP.md` indicates `time_prob1_MPI.csv` is an output. Assuming existing `result_*.csv` or `time_*.csv` in the problem directories or tests directory are the desired raw data.
      I will look for `P5_2_times.csv`, `P5_3_times.csv`, `P5_4_times.csv` in `tests/Project_5_Tests/Autograder/` and `Problem_2`, `Problem_3`, `Problem_4` directories.
      For Problem 1, the `ROADMAP.md` specifies outputting `time_prob1_MPI.csv`. However, the provided project structure doesn't show these specific files directly. I will assume the `P5_X_times.csv` files generated by the autograder (if they exist and cover all configurations) are the primary data sources, and if not, a later task will need to generate them explicitly.
      For now, I'll consolidate any existing `P5_X_times.csv` or similar `time_*.csv` files that represent the raw timing data for the problems into `reports/data/`.

      Specifically:
      - `tests/Project_5_Tests/Autograder/P5_2_times.csv` -> `reports/data/problem2_times.csv`
      - `tests/Project_5_Tests/Autograder/P5_3_times.csv` -> `reports/data/problem3_times.csv`
      - `tests/Project_5_Tests/Autograder/P5_4_times.csv` -> `reports/data/problem4_times.csv`
      - Problem 1's `pingpong_MPI` code would ideally generate a `time_prob1_MPI.csv`. I'll create placeholder files for Problem 1's `time_prob1_MPI_diffnode.csv` and `time_prob1_MPI_samenode.csv` in `reports/data/` as empty files. If actual data is available, it should be copied. If not, a future task will generate it. For now, this task focuses on *consolidation* of what's present.
  </action>
  <verify>
    <automated>
      ls reports/data/problem2_times.csv && 
      ls reports/data/problem3_times.csv && 
      ls reports/data/problem4_times.csv
    </automated>
  </verify>
  <done>
    Relevant timing CSV files are present in `reports/data/`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create analysis utility script</name>
  <files>
    reports/analysis_utils.py
  </files>
  <action>
    Create a Python script `reports/analysis_utils.py` that will contain helper functions for reading CSV files, parsing data, and calculating speedup and efficiency.
    Include initial function definitions for:
    - `read_timing_csv(file_path)`: Reads a CSV into a pandas DataFrame.
    - `calculate_speedup(df, baseline_col='P1_time', parallel_col='Pn_time')`: Calculates speedup.
    - `calculate_efficiency(df, speedup_col='Speedup', num_processors_col='P')`: Calculates efficiency.
    Add necessary imports like `pandas`.
  </action>
  <verify>
    <automated>python -c "import pandas; from reports.analysis_utils import read_timing_csv, calculate_speedup, calculate_efficiency"</automated>
  </verify>
  <done>
    `reports/analysis_utils.py` exists with initial function definitions and necessary imports, and can be imported without error.
  </done>
</task>

</tasks>

<verification>
The `reports/` directory structure is correctly established, relevant timing data files are consolidated, and a Python utility script for common analysis tasks is created and importable.
</verification>

<success_criteria>
- The `reports/` and `reports/data/` directories are successfully created.
- The `problem2_times.csv`, `problem3_times.csv`, and `problem4_times.csv` files are successfully copied into `reports/data/`.
- The `reports/analysis_utils.py` script is created and contains placeholder functions `read_timing_csv`, `calculate_speedup`, and `calculate_efficiency`, and it can be imported successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/05-report-submission/05-01-SUMMARY.md`
</output>
