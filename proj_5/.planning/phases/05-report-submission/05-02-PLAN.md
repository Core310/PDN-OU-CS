---
phase: 05-report-submission
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - reports/problem1_analysis.py
  - reports/problem_summary_tables.py
  - reports/problem1_plot_diffnode.png
  - reports/problem1_plot_samenode.png
  - reports/problem2_summary_table.md
  - reports/problem3_summary_table.md
  - reports/problem4_summary_table.md
autonomous: true
requirements:
  - REPORT-02
  - REPORT-03
user_setup: []

must_haves:
  truths:
    - "Problem 1 analysis script correctly calculates latency and bandwidth via linear regression."
    - "Problem 1 plots for different and same node communication are generated as PNG files."
    - "Summary tables for Problems 2, 3, and 4 are generated in Markdown format, showing wall-clock time, speedup, and efficiency."
  artifacts:
    - path: "reports/problem1_analysis.py"
      provides: "Script for Problem 1 data processing, plotting, and linear regression"
      min_lines: 50
    - path: "reports/problem_summary_tables.py"
      provides: "Script for generating summary tables for Problems 2, 3, 4"
      min_lines: 40
    - path: "reports/problem1_plot_diffnode.png"
      provides: "Plot for Problem 1 (different nodes)"
    - path: "reports/problem1_plot_samenode.png"
      provides: "Plot for Problem 1 (same node)"
    - path: "reports/problem2_summary_table.md"
      provides: "Markdown table for Problem 2 performance metrics"
    - path: "reports/problem3_summary_table.md"
      provides: "Markdown table for Problem 3 performance metrics"
    - path: "reports/problem4_summary_table.md"
      provides: "Markdown table for Problem 4 performance metrics"
  key_links:
    - from: "reports/problem1_analysis.py"
      to: "reports/data/problem1_times_diffnode.csv"
      via: "read_timing_csv"
      pattern: "read_timing_csv"
    - from: "reports/problem_summary_tables.py"
      to: "reports/data/problem2_times.csv"
      via: "read_timing_csv"
      pattern: "read_timing_csv"
    - from: "reports/analysis_utils.py"
      to: "reports/problem1_analysis.py"
      via: "import"
      pattern: "from reports.analysis_utils"
    - from: "reports/analysis_utils.py"
      to: "reports/problem_summary_tables.py"
      via: "import"
      pattern: "from reports.analysis_utils"
---

<objective>
Generate all required performance metrics, tables, and plots for the final report.

Purpose: To visually and numerically summarize the performance of the MPI implementations, providing the core analytical content for the final report.
Output: PNG plots for Problem 1, and Markdown formatted summary tables for Problems 2, 3, and 4.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-report-submission/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Problem 1 analysis, plotting, and linear regression</name>
  <files>
    reports/problem1_analysis.py
    reports/problem1_plot_diffnode.png
    reports/problem1_plot_samenode.png
  </files>
  <action>
    Create `reports/problem1_analysis.py`.
    - Import `reports.analysis_utils` and `matplotlib.pyplot` and `numpy`.
    - Load `reports/data/problem1_times_diffnode.csv` and `reports/data/problem1_times_samenode.csv`.
    - For each dataset, plot array size vs. one-way transmission time.
    - Perform linear regression on the data points to estimate latency (intercept) and bandwidth (slope inverse).
    - Save the plots as `reports/problem1_plot_diffnode.png` and `reports/problem1_plot_samenode.png`.
    - Print the estimated latency and bandwidth values to standard output.
    Ensure to handle the case where `problem1_times_diffnode.csv` or `problem1_times_samenode.csv` might be empty or not fully populated from the previous task. The script should run without error, producing plots even if the data is minimal (e.g., just headers).
  </action>
  <verify>
    <automated>python reports/problem1_analysis.py && ls reports/problem1_plot_diffnode.png && ls reports/problem1_plot_samenode.png</automated>
  </verify>
  <done>
    `reports/problem1_analysis.py` successfully executes, generates two PNG plots, and prints latency/bandwidth estimates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement summary table generation for Problems 2, 3, and 4</name>
  <files>
    reports/problem_summary_tables.py
    reports/problem2_summary_table.md
    reports/problem3_summary_table.md
    reports/problem4_summary_table.md
  </files>
  <action>
    Create `reports/problem_summary_tables.py`.
    - Import `reports.analysis_utils`.
    - Load `reports/data/problem2_times.csv`, `reports/data/problem3_times.csv`, and `reports/data/problem4_times.csv`.
    - For each problem's data:
        - Use `calculate_speedup` and `calculate_efficiency` from `analysis_utils`.
        - Format the wall-clock times, speedup, and efficiency into a Markdown table.
        - Save each table to a separate Markdown file:
            - `reports/problem2_summary_table.md`
            - `reports/problem3_summary_table.md`
            - `reports/problem4_summary_table.md`
    Assume the CSVs will contain columns suitable for `baseline_col` and `parallel_col` for speedup/efficiency calculations (e.g., the first column is baseline, subsequent columns are parallel). If these are not explicitly defined in the CSVs, make reasonable assumptions (e.g., the first column is baseline, subsequent columns are parallel).
  </action>
  <verify>
    <automated>python reports/problem_summary_tables.py && ls reports/problem2_summary_table.md && ls reports/problem3_summary_table.md && ls reports/problem4_summary_table.md</automated>
  </verify>
  <done>
    `reports/problem_summary_tables.py` successfully executes and generates three Markdown table files.
  </done>
</task>

</tasks>

<verification>
All analysis scripts execute without error, plots for Problem 1 are generated and saved, and Markdown summary tables for Problems 2, 3, and 4 are created.
</verification>

<success_criteria>
- `reports/problem1_analysis.py` is created, executes, and outputs two PNG plot files.
- `reports/problem_summary_tables.py` is created, executes, and outputs three Markdown table files.
</success_criteria>

<output>
After completion, create `.planning/phases/05-report-submission/05-02-SUMMARY.md`
</output>
