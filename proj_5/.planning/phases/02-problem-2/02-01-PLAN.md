---
phase: 02-problem-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Problem_2/dot_product_MPI.c]
autonomous: true
requirements: [REQ-2.1, REQ-2.2, REQ-2.3, REQ-2.4, REQ-2.5]

must_haves:
  truths:
    - "MPI_Scatter and MPI_Reduce used for parallelization"
    - "Precision of %.6f for results and times"
    - "No // comments in the code"
    - "Quoted Project_5_Instructions.pdf on Page 6"
  artifacts:
    - path: "Problem_2/dot_product_MPI.c"
      provides: "Parallel dot product implementation"
  key_links:
    - from: "Problem_2/dot_product_MPI.c"
      to: "MPI_Scatter"
      via: "Collective communication for distribution"
    - from: "Problem_2/dot_product_MPI.c"
      to: "MPI_Reduce"
      via: "Collective communication for aggregation"
---

<objective>
Implement a parallel dot product using MPI collective communication.
The implementation must follow strict CLI and output formatting requirements, avoid C99-style comments, and include traceability to the instructions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-problem-2/02-RESEARCH.md
@Problem_2/dot_product_MPI.c
@Problem_2/serial/dotprod_serial.c
@tests/Project_5_Tests/Autograder/autograder_problem_5_2.py

<interfaces>
From Problem_2/serial/dotprod_serial.c:
- Input parsing: strtol(argv[1], NULL, 10)
- File reading: fgets/sscanf loop for CSV values
- Dot product loop: sum += a[i] * b[i]
- Output: fprintf with specified formats
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup, Input, and Memory Management</name>
  <files>Problem_2/dot_product_MPI.c</files>
  <action>
    - Parse CLI arguments (n_items, input1, input2, result_csv, time_csv).
    - On Rank 0: Allocate global vectors of size n_items and read from input CSV files using the pattern from serial/dotprod_serial.c.
    - On all Ranks: Calculate chunk_size = n_items / comm_size.
    - On all Ranks: Allocate local buffers for chunks of size chunk_size.
    - Use /* */ comments only; no // comments allowed.
  </action>
  <verify>
    <automated>mpicc -g -Wall -o Problem_2/dot_product_MPI Problem_2/dot_product_MPI.c</automated>
  </verify>
  <done>Binary compiles and rank 0 reads input vectors successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Parallel Implementation with Scatter/Reduce</name>
  <files>Problem_2/dot_product_MPI.c</files>
  <action>
    - Include quote: "Start the timer after reading in the two arrays and stop the timer after the dot product is computed." (Project_5_Instructions.pdf, Page 6).
    - Measure start time using MPI_Wtime() on Rank 0.
    - Use MPI_Scatter to distribute global vectors from Rank 0 to local buffers on all ranks.
    - Compute local partial dot product sum.
    - Use MPI_Reduce with MPI_SUM to aggregate partial sums on Rank 0.
    - Measure stop time using MPI_Wtime() on Rank 0.
    - On Rank 0: Write the global dot product to argv[4] and elapsed time to argv[5] using %.6f precision.
    - Ensure proper cleanup with free() and MPI_Finalize().
  </action>
  <verify>
    <automated>mpicc -g -Wall -o Problem_2/dot_product_MPI Problem_2/dot_product_MPI.c && mpirun -n 2 ./Problem_2/dot_product_MPI 262144 tests/Project_5_Tests/test_data/Vectors/vec1_2^18.csv tests/Project_5_Tests/test_data/Vectors/vec2_2^18.csv Problem_2/res_test.csv Problem_2/time_test.csv</automated>
  </verify>
  <done>Binary executes correctly on 2 processes and produces formatted output CSVs.</done>
</task>

</tasks>

<verification>
Check for accuracy and precision by running the autograder.
</verification>

<success_criteria>
- Parallel dot product produces accurate results on 2, 4, 8 processes.
- All output values use %.6f precision.
- No // comments are present in dot_product_MPI.c.
- Binary handles n_items 2^18, 2^19, 2^20 correctly.
</success_criteria>

<output>
After completion, create .planning/phases/02-problem-2/02-01-SUMMARY.md
</output>
